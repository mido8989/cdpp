<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundaimotors.hmb.cdppapp.mapper.IFHMBSAPCDPP0005Mapper">
    
    <insert id="insertObject" parameterType="IFHMBSAPCDPP0005Dto" useGeneratedKeys="true" keyProperty="rowId" keyColumn="row_id">
        insert into landing.if_account_ws_wf
            (
                created
                , created_by
                , last_upd
                , last_upd_by
                integrationid
                cnpjnumber
                name
                nickname
                parentaccountname
                agencycode
                fleetacconttype
                industrytrend
                numberOfEmployee
                totalFleetSize
                contractedSince
                Source
                organizationType
                mainPhoneNumber
                mainFaxNumber
                mainEmailAddress
                homepage
                StreetAddress
                StreetAddress2
                StreetAddress3
                County
                City
                State
                PostalCode
            )
        values
            (
                now()
                , 'interface'
                , now()
                , 'interface'

            );
    </insert>   
    
    <insert id="insertProcessAccount" statementType="CALLABLE" parameterType="hashMap">
        call process.hmb_account_webservice_wf(#{PARAM_ID,jdbcType=VARCHAR,mode=IN}, #{checkcu,jdbcType=VARCHAR,mode=IN});
    </insert>

    <update id="updateProcessAccount" statementType="CALLABLE" parameterType="hashMap">
        call process.hmb_account_webservice_wf(#{PARAM_ID,jdbcType=VARCHAR,mode=IN}, #{checkcu,jdbcType=VARCHAR,mode=IN});
    </update>

    <insert id="insertReplicaAccount" statementType="CALLABLE" parameterType="hashMap">
        call replica.hmb_account_webservice_wf(#{PARAM_ID,jdbcType=VARCHAR,mode=IN}, #{checkcu,jdbcType=VARCHAR,mode=IN});
    </insert>

    <update id="updateReplicaAccount" statementType="CALLABLE" parameterType="hashMap">
        call replica.hmb_account_webservice_wf(#{PARAM_ID,jdbcType=VARCHAR,mode=IN}, #{checkcu,jdbcType=VARCHAR,mode=IN});
    </update>
</mapper>



	
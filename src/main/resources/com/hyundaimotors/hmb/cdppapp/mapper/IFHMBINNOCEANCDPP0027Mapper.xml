<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundaimotors.hmb.cdppapp.mapper.IFHMBINNOCEANCDPP0027Mapper">
    
    <select id="getParRowCheck" parameterType="IFHMBINNOCEANCDPP0027Dto" resultType="Integer">
        select
            count(par_row_id)
        from
            landing.S_CONTACT_XM
        where
            par_row_id = #{contactId}
    </select>
    
    <insert id="insertObject" parameterType="IFHMBINNOCEANCDPP0027Dto">
        insert into landing.S_CONTACT_XM
        (
            created
            , created_by
            , last_upd
            , last_upd_by
            , par_row_id
            , name
            , type
            , x_app_status
        )
        values
        (
            now()
            , 'interface'
            , now()
            , 'interface'
            , #{contactId}
            , 'interface'
            , 'Hobby'
            , 'Active'
        );
    </insert>

    <update id="updateObject" parameterType="IFHMBINNOCEANCDPP0027Dto">
        update landing.S_CONTACT_XM
        set last_upd = now()
            , last_upd_by = 'interface'
            , x_app_status = 'Active'
        where
            par_row_id = #{contactId}


    </update>
    
</mapper>

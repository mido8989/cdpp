<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundaimotors.hmb.cdppapp.mapper.IFHMBINNOCEANCDPP0027Mapper">
    
    <select id="getParRowCheck" parameterType="IFHMBINNOCEANCDPP0027Dto" resultType="Integer">
        select
            count(par_row_id)
        from
            landing.CX_CNT_BNF_XM
        where
            par_row_id = #{contactId}
    </select>
    
    <insert id="insertObject" parameterType="IFHMBINNOCEANCDPP0027Dto"  useGeneratedKeys="true" keyProperty="rowId" keyColumn="row_id">
        insert into landing.CX_CNT_BNF_XM
        (
            created
            , created_by
            , last_upd
            , last_upd_by
            , par_row_id
            , name
            , type
            , x_src_app
        )
        values
        (
            now()
            , 'interface'
            , now()
            , 'interface'
            , #{contactId}
            , 'interface'
            , 'Type'
            , #{appSource}
        );
    </insert>

    <update id="updateObject" parameterType="IFHMBINNOCEANCDPP0027Dto"  useGeneratedKeys="true" keyProperty="rowId" keyColumn="row_id">
        update landing.cx_cnt_bnf_xm
        set last_upd = now()
            , last_upd_by = 'interface'
            , x_src_app = #{appSource}
        where
            par_row_id = #{contactId}
    </update>

    <insert id="transferProcess" statementType="CALLABLE" parameterType="hashMap">
        call process.hmb_register_in_application_ws(#{PARAM_ID,jdbcType=VARCHAR,mode=IN}, #{checkcu,jdbcType=VARCHAR,mode=IN});
    </insert>

    <select id="getObject" parameterType="IFHMBINNOCEANCDPP0027Dto" resultType="IFHMBINNOCEANCDPP0027Dto"> 
        select 
            par_row_id as contactId
            , x_app_status as loyaltyStatus
        from 
            landing.s_contact_xm
        where 
            par_row_id = #{contactId}
    </select>
    
</mapper>
